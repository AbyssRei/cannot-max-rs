# resources

This directory stores the Rust workspace's local runtime resources.

## Layout

- `monster.csv` — monster metadata used by `ResourceStore`
- `images/` — monster thumbnails and templates

Place the PNG templates here with filenames matching the `原始名称` column in `monster.csv`.
